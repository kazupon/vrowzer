{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/kazupon/vrowzer/schema/vrowzer-manifest.json",
  "title": "VrowzerManifest",
  "description": "Manifest file for vrowzer virtual filesystem setup. Defines the mapping from virtual FS paths to actual source file paths. Generated by `gen:manifest` CLI or automatically by the `Vrowzer()` plugin with `auto: true`.",
  "type": "object",
  "required": ["name", "files"],
  "properties": {
    "name": {
      "type": "string",
      "description": "Display name for the project. Defaults to package.json 'name' field.",
      "examples": ["Vrowzer + Vue", "my-app"]
    },
    "files": {
      "type": "object",
      "description": "Source files shown in the editor and loaded into the virtual FS. Keys are virtual FS paths (e.g. '/index.html'), values are actual file paths relative to the manifest file (path-reference mode) or file contents (content-resolved mode).",
      "additionalProperties": {
        "type": "string"
      },
      "examples": [
        {
          "/index.html": "./index.html",
          "/src/main.ts": "./src/main.ts",
          "/src/App.vue": "./src/App.vue"
        }
      ]
    },
    "nodeModules": {
      "type": "object",
      "description": "Node modules files loaded into the virtual FS under /node_modules/. Not shown in the editor. Keys are virtual FS paths (e.g. '/node_modules/vue/package.json'), values are actual file paths relative to the manifest file. CJS packages are auto-bundled to ESM under /node_modules/.vrowzer-esm/.",
      "additionalProperties": {
        "type": "string"
      },
      "examples": [
        {
          "/node_modules/vue/package.json": "./node_modules/vue/package.json",
          "/node_modules/vue/dist/vue.runtime.esm-bundler.js": "./node_modules/vue/dist/vue.runtime.esm-bundler.js",
          "/node_modules/.vrowzer-esm/react.js": "./node_modules/.vrowzer-esm/react.js"
        }
      ]
    },
    "vendor": {
      "type": "object",
      "description": "Pre-bundled vendor/runtime files (legacy). Superseded by nodeModules for new projects.",
      "additionalProperties": {
        "type": "string"
      },
      "deprecated": true
    },
    "activeFile": {
      "type": "string",
      "description": "Default file to open in the editor. Must be a key from the 'files' object. Auto-detected from common patterns (App.vue, App.tsx, main.ts, index.html).",
      "examples": ["/src/App.vue", "/src/main.ts", "/index.html"]
    }
  },
  "additionalProperties": false
}
