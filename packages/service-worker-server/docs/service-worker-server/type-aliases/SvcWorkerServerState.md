# Type Alias: SvcWorkerServerState

Extend ServiceWorkerState with additional states

- 'suspended': State when service worker is suspended.

## Signature

```ts
export type SvcWorkerServerState = ServiceWorkerState | "suspended"
```
